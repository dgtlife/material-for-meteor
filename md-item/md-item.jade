//- @file Defines the template for MD Item
    @author Derek Gransaull <derek@dgtlife.com>
    @copyright DGTLife, LLC 2015

    Created on 10/13/2015

//-/////////////////////////////  MD ITEM //////////////////////////////////////
//- MD Item is a generic list item (cf. paper-item/paper-icon-item). An MD item
      * can include an MD icon
      * can ripple when clicked
      * can display a tooltip on hover (item must have an id to enable tooltip)
      * can be an item in an MD menu
      * uses the 'mdItem' template which supports the following parameters.

      @param {string} [id] - the id of the item
      @param {string} [name] - the name of the item
      @param {string} [classes] - a space-separate list of classes of the item
      @param {string} text - the text content of the item
      @param {string} [menuItem] - indicates that it is a menu item
      @param {string} [menu] - the id of the menu that the item is in
      @param {string} [disabled] - "" disables the item
      @param {string} [icon] - the icon id of an icon to include in the item
      @param {string} [textClasses] - a space-separate list of classes of the
                                      item text
      @param {boolean} [ripple] - true if the item should ripple on click
      @param {string} [offsetRipple] - to set the origin of the ripple
      @param {boolean} [hasTooltip] - true if the item should have a tooltip
      @param {string} [tooltipPosition] - true if the item should have a tooltip
      @param {string} [tooltipText] - the text if the item should have a tooltip

template(name="mdItem")
  .md-item(
    data-item=""
    id=id
    name=name
    class=classes
    data-value=text
    data-menu-item=menuItem
    data-menu=menu
    data-disabled=disabled
  )
    if icon
      +mdIcon(icon=icon classes="md-item__icon")
    .md-item__text(class=textClasses)=text
    if ripple
      +mdRipple(offset=offsetRipple)
  if hasTooltip
    +mdTooltip(target=id position=tooltipPosition text=tooltipText)
